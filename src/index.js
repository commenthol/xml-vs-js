const toJs = require('./toJs')
const toXml = require('./toXml')
const {
  TEXT,
  ELEMS,
  ATTRIBS,
  PROCESSING,
  CDATA,
  COMMENT
} = require('./common')

module.exports = {
  toJs,
  toXml,
  TEXT,
  ELEMS,
  ATTRIBS,
  PROCESSING,
  CDATA,
  COMMENT
}
